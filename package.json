{
  "name": "jwt-callback",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "ngrok": "twilio-run --ngrok",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "googleapis": "^67.0.0",
    "lodash": "^4.17.20"
  },
  "devDependencies": {
    "twilio-run": "^2.6.0"
  },
  "engines": {
    "node": "10"
  }
}